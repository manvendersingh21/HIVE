//! Skill system — TOML-defined custom skills with triggers and parameters.

/// Registry of loaded skills.
pub struct SkillRegistry {
    // TODO: HashMap<String, Skill>
}

impl SkillRegistry {
    /// Create an empty skill registry.
    pub fn new() -> Self {
        Self {}
    }
}
