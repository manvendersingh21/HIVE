//! Fine-tuning data collection and export.

/// Collects training data from successful agent interactions.
pub struct DataCollector {
    // TODO: SqlitePool
}

impl DataCollector {
    /// Create a new data collector.
    pub fn new() -> Self {
        Self {}
    }
}
